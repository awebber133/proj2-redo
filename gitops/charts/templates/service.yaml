apiVersion: v1
kind: Service
metadata:
  name: {{ include "proj2.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "proj2.name" . }}
